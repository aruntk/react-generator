import * as React from 'react';
{{#if connectedComponent}}
import { connect } from 'react-redux';
import { RootStateType } from '../../models/type';
import { Dispatch } from 'redux';
{{/if}}

interface {{ properCase name }}Props {
{{#if connectedComponent}}
  dispatch: Dispatch<{}>;
{{/if}}
};
interface {{ properCase name }}State {
};
class {{ properCase name }} extends React.Component<{{ properCase name }}Props, {{ properCase name }}State> {
  state = {};
  render() {
    return (
      <div>
      </div>
    );
  }
}

{{#if connectedComponent}}
const mapState = (state: RootStateType) => ({
});

export default connect(mapState)({{ properCase name }});
{{else}}
export default {{ properCase name }};
{{/if}}
